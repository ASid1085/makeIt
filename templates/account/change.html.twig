{% extends 'layouts/base.html.twig' %}

{% block title 'Change User Password' %}

{% block body %}
    <div class="row">
        <div class="col-md-6 mx-auto">
            <h1 class="my-5 text-center">Modification du mot de passe</h1>
            {{ form_start(resetPasswordForm) }}

                {{ form_widget(resetPasswordForm) }}
                  
                <div class="d-grid">
                    <button type="submit" class="btn btn-lg btn-custom text-beige mt-3" formnovalidate>Changer le mot de passe</button>
                </div>
            {{ form_end(resetPasswordForm) }}
        </div>
    </div>
{% endblock %}
